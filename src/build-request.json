{
  "kind": "build_request",
  "title": "Update page title and Open Graph metadata",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Update `frontend/index.html` `<head>` metadata to match the provided screenshot guidance by (1) changing the document `<title>` to `For You ❤️`, and (2) adding Open Graph meta tags for `og:title`, `og:description`, and `og:type` with the following values:\n- `og:title`: `For You ❤️`\n- `og:description`: `Something special, made with love 💞`\n- `og:type`: `website`\nAlso remove the current long title text so the page does not include words like “interactive”, “project”, or “demo” in the title.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "I need to make one change"
        ]
      },
      "acceptanceCriteria": [
        "`frontend/index.html` `<title>` is exactly `For You ❤️`.",
        "`frontend/index.html` contains `<meta property=\"og:title\" content=\"For You ❤️\">`.",
        "`frontend/index.html` contains `<meta property=\"og:description\" content=\"Something special, made with love 💞\">`.",
        "`frontend/index.html` contains `<meta property=\"og:type\" content=\"website\">`.",
        "The previous title `Romantic Valentine Proposal Single-Page Experience with Interactive Yes/No Buttons` no longer appears anywhere in `frontend/index.html`."
      ]
    }
  ],
  "constraints": [
    "Do not modify any files under `frontend/src/components/ui` (compose them instead).",
    "Do not edit files listed in `frontend.immutablePaths` in SYSTEM_CONTEXT."
  ],
  "nonGoals": [
    "Adding new backend functionality or persistence.",
    "Adding additional social metadata tags (e.g., Twitter cards) beyond what is requested."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}
{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Update HTML title and Open Graph metadata",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Update the HTML document title and add required Open Graph meta tags in frontend/index.html.",
      "acceptanceCriteria": [
        "`frontend/index.html` `<title>` is exactly `For You ❤️`.",
        "`frontend/index.html` contains `<meta property=\"og:title\" content=\"For You ❤️\">`.",
        "`frontend/index.html` contains `<meta property=\"og:description\" content=\"Something special, made with love 💞\">`.",
        "`frontend/index.html` contains `<meta property=\"og:type\" content=\"website\">`.",
        "The previous title `Romantic Valentine Proposal Single-Page Experience with Interactive Yes/No Buttons` no longer appears anywhere in `frontend/index.html`."
      ],
      "file_operations": [
        {
          "path": "frontend/index.html",
          "operation": "modify",
          "description": "Within <head>, replace the existing long <title> with exactly `For You ❤️`, add Open Graph tags for og:title/og:description/og:type with the specified values, and ensure the old long title string is removed from the file."
        }
      ]
    }
  ]
}